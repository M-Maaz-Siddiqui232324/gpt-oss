# Core dependencies
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0

# API & Web UI
fastapi>=0.100.0
uvicorn>=0.23.0
streamlit>=1.25.0
pydantic>=2.0.0
itsdangerous>=2.0.0  


# Database
psycopg2-binary>=2.9.0  

# Document processing
python-docx>=0.8.11

# Note: Ollama must be installed separately
# Install: curl -fsSL https://ollama.com/install.sh | sh
# Then: ollama pull gpt-oss:20b

# Note: Set SECRET_KEY environment variable for session security
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"